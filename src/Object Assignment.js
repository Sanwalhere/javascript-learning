//--------------------------------------------------|Object Assignment|--------------------------------------------------------

let user = { name: "Ali" };

let a = { see: true, watch: false };
let b = { speak: false };

let sanwal = Object.assign(user, a, b);

console.log(sanwal);
