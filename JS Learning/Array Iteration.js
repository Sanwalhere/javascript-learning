//--------------------------------------------------|Array Iteration|---------------------------------------------------------

const details = ["ios", "golden", "32gb", "1tb"];
for (let index in details) {
  console.log(index, details[index]);
}
